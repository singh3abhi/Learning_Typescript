"use strict";
/*

We will generate ts config file
tsc -init

After confguring the root and outDir then we will start the watcher

tsc -w

This folder structure is rarely used as the typescript is used with react or node js and they will have
their own respective folder structures
*/
const a = 26;
alert(a);
console.log(a);
